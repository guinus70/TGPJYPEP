<div class ='text-center'>
  <div class="card">
    <div class="card-body">
      <h5 class="card-title"><%= @user.first_name %> <%= @user.last_name %></h5>
      <p class="card-title"><strong>Age : </strong> <%= @user.age %></p>
      <p class="card-title"><%= link_to @user_city.name, city_path(:id => @user_city.id) %></p>
      <p class="card-text"> <strong>Bio :</strong><%= @user.description %></p>
      <p class="card-text"> <strong>A rejoint The Gossip Project le : </strong><%= @user.created_at.strftime("%d/%m/%Y") %></p>
      <p class="card-text"> <strong>Le nombre de gossip(s) partagés : </strong><%= @user_gossip_count %></p>
    </div>
  </div>
</div>